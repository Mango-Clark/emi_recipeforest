plugins {
    id 'multiloader-common'
    id 'net.neoforged.moddev.legacyforge'
}

legacyForge {
    mcpVersion = minecraft_version
    // Automatically enable AccessTransformers if the file exists
    def at = file('src/main/resources/META-INF/accesstransformer.cfg')
    if (at.exists()) {
        accessTransformers = [at.absolutePath]
    }
    parchment {
        minecraftVersion = parchment_minecraft
        mappingsVersion = parchment_version
    }
}

dependencies {
    compileOnly "dev.emi:emi-xplat-mojmap:${emi_version}"
    compileOnly group: 'org.spongepowered', name: 'mixin', version: '0.8.5'
    compileOnly group: 'org.ow2.asm', name: 'asm-tree', version: '9.7'
    // Fabric and Forge both bundle MixinExtras, so it is safe to use it in common.
    compileOnly group: 'io.github.llamalad7', name: 'mixinextras-common', version: '0.3.5'
    annotationProcessor group: 'io.github.llamalad7', name: 'mixinextras-common', version: '0.3.5'
    testImplementation platform('org.junit:junit-bom:5.10.3')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

tasks.named('test', Test).configure {
    useJUnitPlatform()
}

def emiCompatibilityContracts = [
        'dev.emi.emi.bom.BoM': [
                [token: ' tree;', descriptor: 'Ldev/emi/emi/bom/MaterialTree;', access: 'public static'],
                [token: ' craftingMode;', descriptor: 'Z', access: 'public static'],
                [token: ' setGoal(', descriptor: '(Ldev/emi/emi/api/recipe/EmiRecipe;)V', access: 'public static']
        ],
        'dev.emi.emi.bom.MaterialTree': [
                [token: 'MaterialTree(', descriptor: '(Ldev/emi/emi/api/recipe/EmiRecipe;)V', access: 'public'],
                [token: ' goal;', descriptor: 'Ldev/emi/emi/bom/MaterialNode;', access: 'public'],
                [token: ' resolutions;', descriptor: 'Ljava/util/Map;', access: 'public'],
                [token: ' batches;', descriptor: 'J', access: 'public'],
                [token: ' addResolution(', descriptor: '(Ldev/emi/emi/api/stack/EmiIngredient;Ldev/emi/emi/api/recipe/EmiRecipe;)V', access: 'public']
        ],
        'dev.emi.emi.bom.MaterialNode': [
                [token: ' recipe;', descriptor: 'Ldev/emi/emi/api/recipe/EmiRecipe;', access: 'public'],
                [token: ' children;', descriptor: 'Ljava/util/List;', access: 'public'],
                [token: ' state;', descriptor: 'Ldev/emi/emi/bom/FoldState;', access: 'public']
        ],
        'dev.emi.emi.widget.RecipeTreeButtonWidget': [
                [token: 'RecipeTreeButtonWidget(', descriptor: '(IILdev/emi/emi/api/recipe/EmiRecipe;)V', access: 'public'],
                [token: ' mouseClicked(', descriptor: '(III)Z', access: 'public'],
                [token: ' getTooltip(', descriptor: '(II)Ljava/util/List;', access: 'public']
        ],
        'dev.emi.emi.runtime.EmiReloadManager': [
                [token: ' clear(', descriptor: '()V', access: 'public static'],
                [token: ' reload(', descriptor: '()V', access: 'public static'],
                [token: ' isLoaded(', descriptor: '()Z', access: 'public static']
        ],
        'dev.emi.emi.runtime.EmiFavorites': [
                [token: ' updateSynthetic(', descriptor: '(Ldev/emi/emi/api/recipe/EmiPlayerInventory;)V', access: 'public static']
        ],
        'dev.emi.emi.runtime.EmiSidebars': [
                [token: ' getStacks(', descriptor: '(Ldev/emi/emi/config/SidebarType;)Ljava/util/List;', access: 'public static']
        ],
        'dev.emi.emi.screen.widget.EmiSearchWidget': [
                [token: ' keyPressed(', descriptor: '(III)Z', access: 'public']
        ],
        'dev.emi.emi.screen.ConfigScreen': [
                [token: ' list;', descriptor: 'Ldev/emi/emi/screen/widget/config/ListWidget;', access: 'public'],
                [token: ' activeBind;', descriptor: 'Ldev/emi/emi/input/EmiBind;', access: 'public'],
                [token: ' search;', descriptor: 'Ldev/emi/emi/screen/widget/config/ConfigSearch;', access: 'private'],
                [token: ' init(', descriptor: '()V', access: 'protected'],
                [token: ' addJumpButtons(', descriptor: '()V', access: 'private'],
                [token: ' jump(', descriptor: '(Ljava/lang/String;)V', access: 'public'],
                [token: ' mouseClicked(', descriptor: '(DDI)Z', access: 'public'],
                [token: ' keyPressed(', descriptor: '(III)Z', access: 'public'],
                [token: ' keyReleased(', descriptor: '(III)Z', access: 'public']
        ],
        'dev.emi.emi.screen.ConfigEnumScreen': [
                [token: 'ConfigEnumScreen(', descriptor: '(Ldev/emi/emi/screen/ConfigScreen;Ljava/util/List;Ljava/util/function/Consumer;)V', access: 'public']
        ],
        'dev.emi.emi.screen.ConfigEnumScreen$Entry': [
                [token: 'ConfigEnumScreen$Entry(', descriptor: '(Ljava/lang/Object;Lnet/minecraft/network/chat/Component;Ljava/util/List;)V', access: 'public']
        ],
        'dev.emi.emi.screen.widget.config.ListWidget': [
                [token: ' children(', descriptor: '()Ljava/util/List;', access: 'public'],
                [token: ' addEntry(', descriptor: '(Ldev/emi/emi/screen/widget/config/ListWidget$Entry;)I', access: 'public']
        ],
        'dev.emi.emi.screen.widget.config.GroupNameWidget': [
                [token: 'GroupNameWidget(', descriptor: '(Ljava/lang/String;Lnet/minecraft/network/chat/Component;)V', access: 'public'],
                [token: ' id;', descriptor: 'Ljava/lang/String;', access: 'public'],
                [token: ' children;', descriptor: 'Ljava/util/List;', access: 'public'],
                [token: ' collapsed;', descriptor: 'Z', access: 'public']
        ],
        'dev.emi.emi.screen.widget.config.ConfigEntryWidget': [
                [token: 'ConfigEntryWidget(', descriptor: '(Lnet/minecraft/network/chat/Component;Ljava/util/List;Ljava/util/function/Supplier;I)V', access: 'public'],
                [token: ' setChildren(', descriptor: '(Ljava/util/List;)V', access: 'public'],
                [token: ' update(', descriptor: '(IIII)V', access: 'public'],
                [token: ' render(', descriptor: '(Lnet/minecraft/client/gui/GuiGraphics;IIIIIIIZF)V', access: 'public'],
                [token: ' getTooltip(', descriptor: '(II)Ljava/util/List;', access: 'public'],
                [token: ' parentGroups;', descriptor: 'Ljava/util/List;', access: 'public']
        ],
        'dev.emi.emi.screen.widget.config.ConfigSearch': [
                [token: ' getSearch(', descriptor: '()Ljava/lang/String;', access: 'public']
        ],
        'dev.emi.emi.screen.widget.config.ConfigJumpButton': [],
        'dev.emi.emi.screen.widget.config.EmiBindWidget': [
                [token: 'EmiBindWidget(', descriptor: '(Ldev/emi/emi/screen/ConfigScreen;Ljava/util/List;Ljava/util/function/Supplier;Ldev/emi/emi/input/EmiBind;)V', access: 'public'],
                [token: ' update(', descriptor: '(IIII)V', access: 'public']
        ],
        'dev.emi.emi.screen.widget.config.IntEdit': [
                [token: 'IntEdit(', descriptor: '(ILjava/util/function/IntSupplier;Ljava/util/function/IntConsumer;)V', access: 'public'],
                [token: ' text;', descriptor: 'Lnet/minecraft/client/gui/components/EditBox;', access: 'public final'],
                [token: ' up;', descriptor: 'Lnet/minecraft/client/gui/components/Button;', access: 'public final'],
                [token: ' down;', descriptor: 'Lnet/minecraft/client/gui/components/Button;', access: 'public final'],
                [token: ' setPosition(', descriptor: '(II)V', access: 'public']
        ],
        'dev.emi.emi.screen.RecipeScreen': [
                [token: 'RecipeScreen(', descriptor: '(Lnet/minecraft/client/gui/screens/inventory/AbstractContainerScreen;Ljava/util/Map;)V', access: 'public'],
                [token: ' onClose(', descriptor: '()V', access: 'public'],
                [token: ' resolve;', descriptor: 'Ldev/emi/emi/api/stack/EmiIngredient;', access: 'public static']
        ],
        'dev.emi.emi.api.EmiApi': [
                [token: ' viewRecipeTree(', descriptor: '()V', access: 'public static'],
                [token: ' getHandledScreen(', descriptor: '()Lnet/minecraft/client/gui/screens/inventory/AbstractContainerScreen;', access: 'public static'],
                [token: ' getRecipeManager(', descriptor: '()Ldev/emi/emi/api/recipe/EmiRecipeManager;', access: 'public static']
        ],
        'dev.emi.emi.api.recipe.EmiRecipeManager': [
                [token: ' getRecipesByOutput(', descriptor: '(Ldev/emi/emi/api/stack/EmiStack;)Ljava/util/List;', access: 'public'],
                [token: ' getRecipe(', descriptor: '(Lnet/minecraft/resources/ResourceLocation;)Ldev/emi/emi/api/recipe/EmiRecipe;', access: 'public']
        ],
        'dev.emi.emi.api.stack.serializer.EmiIngredientSerializer': [
                [token: ' getSerialized(', descriptor: '(Ldev/emi/emi/api/stack/EmiIngredient;)Lcom/google/gson/JsonElement;', access: 'public static'],
                [token: ' getDeserialized(', descriptor: '(Lcom/google/gson/JsonElement;)Ldev/emi/emi/api/stack/EmiIngredient;', access: 'public static']
        ],
        'dev.emi.emi.api.recipe.EmiResolutionRecipe': [
                [token: 'EmiResolutionRecipe(', descriptor: '(Ldev/emi/emi/api/stack/EmiIngredient;Ldev/emi/emi/api/stack/EmiStack;)V', access: 'public'],
                [token: ' stack;', descriptor: 'Ldev/emi/emi/api/stack/EmiStack;', access: 'public']
        ],
        'dev.emi.emi.api.recipe.EmiRecipe': [
                [token: ' getId(', descriptor: '()Lnet/minecraft/resources/ResourceLocation;', access: 'public'],
                [token: ' supportsRecipeTree(', descriptor: '()Z', access: 'public']
        ],
        'dev.emi.emi.api.stack.EmiIngredient': [
                [token: ' isEmpty(', descriptor: '()Z', access: 'public'],
                [token: ' getEmiStacks(', descriptor: '()Ljava/util/List;', access: 'public']
        ],
        'dev.emi.emi.EmiPort': [
                [token: ' id(', descriptor: '(Ljava/lang/String;)Lnet/minecraft/resources/ResourceLocation;', access: 'public static']
        ],
        'dev.emi.emi.EmiUtil': [
                [token: ' getPreferredRecipe(', descriptor: '(Ljava/util/List;Ldev/emi/emi/api/recipe/EmiPlayerInventory;Z)Ldev/emi/emi/api/recipe/EmiRecipe;', access: 'public static']
        ],
        'dev.emi.emi.EmiRenderHelper': [
                [token: ' drawTooltip(', descriptor: '(Lnet/minecraft/client/gui/screens/Screen;Ldev/emi/emi/runtime/EmiDrawContext;Ljava/util/List;II)V', access: 'public static']
        ],
        'dev.emi.emi.screen.BoMScreen': [
                [token: 'BoMScreen(', descriptor: '(Lnet/minecraft/client/gui/screens/inventory/AbstractContainerScreen;)V', access: 'public'],
                [token: ' old;', descriptor: 'Lnet/minecraft/client/gui/screens/inventory/AbstractContainerScreen;', access: 'public']
        ],
        'dev.emi.emi.screen.widget.SizedButtonWidget': [
                [token: 'SizedButtonWidget(', descriptor: '(IIIIIILjava/util/function/BooleanSupplier;Lnet/minecraft/client/gui/components/Button$OnPress;Ljava/util/List;)V', access: 'public']
        ],
        'dev.emi.emi.screen.EmiScreenManager': [
                [token: ' keyPressed(', descriptor: '(III)Z', access: 'public static'],
                [token: ' genericInteraction(', descriptor: '(Ljava/util/function/Function;)Z', access: 'public static'],
                [token: ' mouseClicked(', descriptor: '(DDI)Z', access: 'public static'],
                [token: ' mouseReleased(', descriptor: '(DDI)Z', access: 'public static'],
                [token: ' mouseDragged(', descriptor: '(DDIDD)Z', access: 'public static'],
                [token: ' repopulatePanels(', descriptor: '(Ldev/emi/emi/config/SidebarType;)V', access: 'public static'],
                [token: ' getHoveredStack(', descriptor: '(IIZ)Ldev/emi/emi/api/stack/EmiStackInteraction;', access: 'public static'],
                [token: ' pressedStack;', descriptor: 'Ldev/emi/emi/api/stack/EmiIngredient;', access: 'public static'],
                [token: ' draggedStack;', descriptor: 'Ldev/emi/emi/api/stack/EmiIngredient;', access: 'public static'],
                [token: ' search;', descriptor: 'Ldev/emi/emi/screen/widget/EmiSearchWidget;', access: 'public static'],
                [token: ' tree;', descriptor: 'Ldev/emi/emi/screen/widget/SizedButtonWidget;', access: 'public static'],
                [token: ' lastPlayerInventory;', descriptor: 'Ldev/emi/emi/api/recipe/EmiPlayerInventory;', access: 'public static'],
                [token: ' lastMouseX;', descriptor: 'I', access: 'public static'],
                [token: ' lastMouseY;', descriptor: 'I', access: 'public static']
        ],
        'dev.emi.emi.config.EmiConfig$ConfigValue': [
                [token: ' value(', descriptor: '()Ljava/lang/String;', access: 'public abstract']
        ],
        'dev.emi.emi.input.EmiBind': [
                [token: 'EmiBind(', descriptor: '(Ljava/lang/String;[Ldev/emi/emi/input/EmiBind$ModifiedKey;)V', access: 'public'],
                [token: ' translationKey;', descriptor: 'Ljava/lang/String;', access: 'public final'],
                [token: ' boundKeys;', descriptor: 'Ljava/util/List;', access: 'public'],
                [token: ' setBind(', descriptor: '(ILdev/emi/emi/input/EmiBind$ModifiedKey;)V', access: 'public'],
                [token: ' setBinds(', descriptor: '([Ldev/emi/emi/input/EmiBind$ModifiedKey;)V', access: 'public'],
                [token: ' setToDefault(', descriptor: '()V', access: 'public'],
                [token: ' matchesKey(', descriptor: '(II)Z', access: 'public'],
                [token: ' matchesMouse(', descriptor: '(I)Z', access: 'public']
        ],
        'dev.emi.emi.input.EmiBind$ModifiedKey': [
                [token: 'EmiBind$ModifiedKey(', descriptor: '(Lcom/mojang/blaze3d/platform/InputConstants$Key;I)V', access: 'public'],
                [token: ' key(', descriptor: '()Lcom/mojang/blaze3d/platform/InputConstants$Key;', access: 'public'],
                [token: ' modifiers(', descriptor: '()I', access: 'public'],
                [token: ' modifiersToMatch(', descriptor: '()I', access: 'public'],
                [token: ' isUnbound(', descriptor: '()Z', access: 'public']
        ],
        'dev.emi.emi.input.EmiInput': [
                [token: ' getCurrentModifiers(', descriptor: '()I', access: 'public static'],
                [token: ' SHIFT_MASK;', descriptor: 'I', access: 'public static']
        ],
        'dev.emi.emi.api.stack.EmiStackInteraction': [
                [token: ' isEmpty(', descriptor: '()Z', access: 'public'],
                [token: ' getRecipeContext(', descriptor: '()Ldev/emi/emi/api/recipe/EmiRecipe;', access: 'public'],
                [token: ' getStack(', descriptor: '()Ldev/emi/emi/api/stack/EmiIngredient;', access: 'public']
        ],
        'dev.emi.emi.runtime.EmiPersistentData': [
                [token: ' load(', descriptor: '()V', access: 'public static']
        ],
        'dev.emi.emi.runtime.EmiFavorite$Synthetic': [
                [token: 'EmiFavorite$Synthetic(', descriptor: '(Ldev/emi/emi/api/stack/EmiIngredient;JJ)V', access: 'public']
        ]
]

tasks.register('verifyEmiCompatibilityMatrix') {
    group = 'verification'
    description = 'Resolves EMI 1.1.13-1.1.24 and verifies RecipeForest JVM contracts.'

    doLast {
        def executableName = System.getProperty('os.name').toLowerCase().contains('windows') ? 'javap.exe' : 'javap'
        def javap = javaToolchains.launcherFor {
            languageVersion = JavaLanguageVersion.of(java_version)
        }.get().metadata.installationPath.file("bin/${executableName}").asFile

        (13..24).each { patch ->
            def version = "1.1.${patch}+1.20.1"
            def artifact = configurations.detachedConfiguration(
                    dependencies.create("dev.emi:emi-xplat-mojmap:${version}"))
            artifact.transitive = false
            def jar = artifact.singleFile
            def contracts = emiCompatibilityContracts.collectEntries { owner, members ->
                [(owner): members.collect { new LinkedHashMap(it) }]
            }
            contracts['dev.emi.emi.runtime.EmiFavorite$Synthetic'] << [
                    token: 'EmiFavorite$Synthetic(',
                    descriptor: patch >= 21
                            ? '(Ldev/emi/emi/api/recipe/EmiRecipe;JJJI)V'
                            : '(Ldev/emi/emi/api/recipe/EmiRecipe;JJI)V',
                    access: 'public'
            ]
            if (patch >= 22) {
                contracts['dev.emi.emi.bom.MaterialNode'] = [[
                        token: ' catalyst;', descriptor: 'Z', access: 'public'
                ]]
            } else {
                contracts['dev.emi.emi.bom.TreeCost'] = [[
                        token: ' isCatalyst(',
                        descriptor: '(Ldev/emi/emi/api/stack/EmiIngredient;)Z',
                        access: 'public static'
                ]]
            }

            contracts.each { owner, members ->
                def output = new ByteArrayOutputStream()
                def result = project.exec {
                    commandLine javap.absolutePath, '-classpath', jar.absolutePath, '-p', '-s', owner
                    standardOutput = output
                    errorOutput = output
                    ignoreExitValue = true
                }
                if (result.exitValue != 0) {
                    throw new GradleException("EMI ${version}: missing class ${owner}: ${output.toString('UTF-8')}")
                }
                def lines = output.toString('UTF-8').readLines()
                if (owner in ['dev.emi.emi.input.EmiBind',
                        'dev.emi.emi.screen.widget.config.EmiBindWidget']) {
                    def declaration = lines.find { it.startsWith('public ') && it.contains(" ${owner} ") }
                    if (declaration == null || declaration.contains(' final class ')
                            || declaration.contains(' interface ')) {
                        throw new GradleException("EMI ${version}: ${owner} must remain a non-final class")
                    }
                }
                if (owner == 'dev.emi.emi.screen.widget.config.ConfigJumpButton') {
                    def declaration = lines.find { it.startsWith('public ') && it.contains(" ${owner} ") }
                    if (declaration == null
                            || !declaration.contains(' extends dev.emi.emi.screen.widget.SizedButtonWidget ')) {
                        throw new GradleException("EMI ${version}: ${owner} must extend SizedButtonWidget")
                    }
                }
                members.each { member ->
                    def found = (0..<lines.size()).any { index ->
                        def signature = lines[index].trim()
                        if (!signature.startsWith(member.access) || !signature.contains(member.token)) {
                            return false
                        }
                        def descriptorLine = ((index + 1)..<lines.size()).find { !lines[it].trim().isEmpty() }
                        return descriptorLine != null
                                && lines[descriptorLine].trim() == "descriptor: ${member.descriptor}"
                    }
                    if (!found) {
                        throw new GradleException("EMI ${version}: missing or incompatible ${owner}"
                                + " ${member.token.trim()} descriptor ${member.descriptor}")
                    }
                }
            }
            def emiApiBytecode = new ByteArrayOutputStream()
            def emiApiResult = project.exec {
                commandLine javap.absolutePath, '-classpath', jar.absolutePath, '-p', '-c', '-s',
                        'dev.emi.emi.api.EmiApi'
                standardOutput = emiApiBytecode
                errorOutput = emiApiBytecode
                ignoreExitValue = true
            }
            if (emiApiResult.exitValue != 0) {
                throw new GradleException("EMI ${version}: unable to inspect EmiApi bytecode: "
                        + emiApiBytecode.toString('UTF-8'))
            }
            def apiLines = emiApiBytecode.toString('UTF-8').readLines()
            def viewTreeStart = apiLines.findIndexOf { it.trim() == 'public static void viewRecipeTree();' }
            def viewTreeEnd = viewTreeStart < 0 ? -1 : ((viewTreeStart + 1)..<apiLines.size()).find {
                def line = apiLines[it]
                line.startsWith('  ') && !line.startsWith('    ') && line.trim().endsWith(';')
            }
            if (viewTreeEnd == null || viewTreeEnd < 0) {
                viewTreeEnd = apiLines.size()
            }
            def newBoMScreenCount = viewTreeStart < 0 ? 0 : apiLines.subList(viewTreeStart, viewTreeEnd).count {
                it.contains('new') && it.contains('// class dev/emi/emi/screen/BoMScreen')
            }
            if (newBoMScreenCount != 2) {
                throw new GradleException("EMI ${version}: EmiApi.viewRecipeTree()V expected exactly two NEW "
                        + "dev/emi/emi/screen/BoMScreen sites, found ${newBoMScreenCount}")
            }
            def screenManagerBytecode = new ByteArrayOutputStream()
            def screenManagerResult = project.exec {
                commandLine javap.absolutePath, '-classpath', jar.absolutePath, '-p', '-c', '-s',
                        'dev.emi.emi.screen.EmiScreenManager'
                standardOutput = screenManagerBytecode
                errorOutput = screenManagerBytecode
                ignoreExitValue = true
            }
            if (screenManagerResult.exitValue != 0) {
                throw new GradleException("EMI ${version}: unable to inspect EmiScreenManager bytecode: "
                        + screenManagerBytecode.toString('UTF-8'))
            }
            def screenManagerLines = screenManagerBytecode.toString('UTF-8').readLines()
            def genericStart = screenManagerLines.findIndexOf {
                it.trim() == 'public static boolean genericInteraction(java.util.function.Function<dev.emi.emi.input.EmiBind, java.lang.Boolean>);'
            }
            def genericEnd = genericStart < 0 ? -1 : ((genericStart + 1)..<screenManagerLines.size()).find {
                def line = screenManagerLines[it]
                line.startsWith('  ') && !line.startsWith('    ') && line.trim().endsWith(';')
            }
            if (genericEnd == null || genericEnd < 0) {
                genericEnd = screenManagerLines.size()
            }
            def viewTreeCallCount = genericStart < 0 ? 0 : screenManagerLines.subList(genericStart, genericEnd).count {
                it.contains('invokestatic') && it.contains('// Method dev/emi/emi/api/EmiApi.viewRecipeTree:()V')
            }
            if (viewTreeCallCount != 1) {
                throw new GradleException("EMI ${version}: EmiScreenManager.genericInteraction(Function)Z expected "
                        + "exactly one EmiApi.viewRecipeTree()V call, found ${viewTreeCallCount}")
            }
            def configBytecode = new ByteArrayOutputStream()
            def configResult = project.exec {
                commandLine javap.absolutePath, '-classpath', jar.absolutePath, '-p', '-c', '-s',
                        'dev.emi.emi.screen.ConfigScreen'
                standardOutput = configBytecode
                errorOutput = configBytecode
                ignoreExitValue = true
            }
            if (configResult.exitValue != 0) {
                throw new GradleException("EMI ${version}: unable to inspect ConfigScreen bytecode: "
                        + configBytecode.toString('UTF-8'))
            }
            def configLines = configBytecode.toString('UTF-8').readLines()
            def jumpStart = configLines.findIndexOf { it.trim() == 'private void addJumpButtons();' }
            def jumpEnd = jumpStart < 0 ? -1 : ((jumpStart + 1)..<configLines.size()).find {
                def line = configLines[it]
                line.startsWith('  ') && !line.startsWith('    ') && line.trim().endsWith(';')
            }
            if (jumpEnd == null || jumpEnd < 0) {
                jumpEnd = configLines.size()
            }
            def newJumpButtonCount = jumpStart < 0 ? 0 : configLines.subList(jumpStart, jumpEnd).count {
                it.contains('new') && it.contains('// class dev/emi/emi/screen/widget/config/ConfigJumpButton')
            }
            if (newJumpButtonCount != 1) {
                throw new GradleException("EMI ${version}: ConfigScreen.addJumpButtons()V expected exactly one NEW "
                        + "ConfigJumpButton site, found ${newJumpButtonCount}")
            }
            logger.lifecycle("Verified EMI ${version}")
        }
    }
}

configurations {
    commonJava {
        canBeResolved = false
        canBeConsumed = true
    }
    commonResources {
        canBeResolved = false
        canBeConsumed = true
    }
}

artifacts {
    commonJava sourceSets.main.java.sourceDirectories.singleFile
    commonResources sourceSets.main.resources.sourceDirectories.singleFile
}
